{
  "name": "classic-paragraph-wit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node --check src/app.js && node --check src/recommendation.js && node --check src/web.js && node --check src/classics.js"
  }
}
